<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nodejs + EJS</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white p-10">
        
        <div class="nav mb-6">
            <a class="text-blue-500" href="/read">View Users</a>                
        </div>     

            <h2 class="text-3xl tracking-tighter mb-3">Edit User</h2>
            <form action="/updateUser/<%= user._id%>" method="post">
                <input class="px-5 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="text"
                    placeholder="Enter Your Name" value="<%= user.name %>" name="name" />
                <input class="px-5 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="text"
                    placeholder="Enter Your Email" value="<%= user.email %>" name="email" />                
                <input class="px-5 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none" type="text"
                    placeholder="Enter Image Url" value="<%= user.image%>" name="image" />                
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Update" />
            </form>
    </div>
</body>

</html>