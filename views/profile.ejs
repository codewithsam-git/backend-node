<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h2 class="text-3xl tracking-tighter mb-3">Hello, <%= users.name%>
        </h2>   
        <a href="/logout">Logout</a>
        <h3 class="mb-5 text-zinc-400">You can create new post</h3>
        <form action="createPost"  method="post">
            <textarea class="block p-3 w-1/3 rounded-md bg-transparent border-2 border-zinc-600 outline-none" type="text"
                placeholder="What's in your mind ?" name="content"></textarea>
                <input class="block bg-blue-500 px-5 py-2 w-40 rounded-md mt-2" type="submit" value="Create Post" />
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Posts</h3>
            <div class="postcontainer mt-5">
                
                    <% users.posts.reverse().forEach(function(value){ %>
                        <div class="post w-1/3 p-3 mb-3 bg-zinc-800 border-2 border-zinc-600 rounded-md">
                            <h4 class="text-blue-500">@<%= users.username %></h4>
                            <p class="text-sm tracking-tight"><%= value.content %></p>
                            <small class="mt-1"><%= value.likes.length %> Likes</small>
                            <div class="btns flex gap-3 mt-1">
                                <a href="/like/<%= value._id%>" class="text-blue-500">
                                    <%= value.likes.indexOf(users._id) == -1  ? "Like" : "Unlike" %>
                                </a>
                                <a href="/editPost/<%= value._id%>" class="text-zinc-500">Edit</a>
                            </div>
                        </div>
                       
                    <% }) %>

            </div>
        </div>
        
    </div>
</body>

</html>